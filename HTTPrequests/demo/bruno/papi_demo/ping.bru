meta {
  name: ping
  type: http
  seq: 2
}

get {
  url: {{host}}/{{base}}/hs/papi/v1/ping/
  body: none
  auth: inherit
}

vars:pre-request {
  host: localhost
  base: papi_demo
}

settings {
  encodeUrl: true
  timeout: 0
}
